{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "vercel-build": "npm install && cp ../../config/firebase-creds.json ./config/ || true"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.18.2",
    "firebase-admin": "^13.3.0",
    "morgan": "^1.10.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}